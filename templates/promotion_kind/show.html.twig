{% extends 'base.html.twig' %}

{% block title %}PromotionKind{% endblock %}

{% block body %}
    <h1>PromotionKind</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion_kind.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ promotion_kind.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('promotion_kind_index') }}">back to list</a>

    <a href="{{ path('promotion_kind_edit', {'id': promotion_kind.id}) }}">edit</a>

    {{ include('promotion_kind/_delete_form.html.twig') }}
{% endblock %}
