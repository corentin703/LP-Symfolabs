<form method="post" action="{{ path('good_plan_delete', {'id': good_plan.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ good_plan.id) }}">
    <button class="btn">Delete</button>
</form>
